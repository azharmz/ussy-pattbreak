import {ensureSchema,currentRun,validCurrent,readPointers} from "./d1/runtime.js";
import {historicalOpportunities} from "./d1/history.js";
import {syncProjectionV9} from "./d1/compact.js";
const json=(x,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
function page(url){const l=Number(url.searchParams.get("limit")??100),o=Number(url.searchParams.get("offset")??0);if(!Number.isInteger(l)||l<1||l>500||!Number.isInteger(o)||o<0)throw Error("invalid pagination");return[l,o]}
async function list(db,url,kind){const[l,o]=page(url),run=await currentRun(db),table=kind==="opportunities"?"dashboard_opportunities_v8":"dashboard_positions_v8",state=kind==="history"?"CLOSED":url.searchParams.get("state"),field="state";let sql=`SELECT payload FROM ${table} WHERE generation=?`,b=[run.generation];if(kind==="opportunities"){const date=url.searchParams.get("signal_date");if(date){sql+=" AND json_extract(payload,'$.as_of_date')=?";b.push(date)}}if(state){sql+=` AND json_extract(payload,'$.${field}')=?`;b.push(state)}else if(kind==="positions")sql+=" AND json_extract(payload,'$.state') IN ('OPEN','EXIT_PENDING')";sql+=" ORDER BY json_extract(payload,'$.as_of_date') DESC,id ASC LIMIT ? OFFSET ?";return(await db.prepare(sql).bind(...b,l,o).all()).results.map(x=>JSON.parse(x.payload))}
async function health(env){const r=await currentRun(env.DB),s=await env.DB.prepare("SELECT json_extract(payload,'$.state') state,COUNT(*) count FROM dashboard_positions_v8 WHERE generation=? GROUP BY state").bind(r.generation).all();return{status:await validCurrent(env.DB,r)?"READY":"EMPTY",mode:env.LOCAL_FIXTURE?"LOCAL_FIXTURE":"PRODUCTION",latest_projection:r,opportunity_count:r?.opportunities??0,opportunity_dates:r?.as_of_date?[{signal_date:r.as_of_date,count:r.opportunities??0}]:[],position_states:s.results}}
export default{async fetch(request,env){const u=new URL(request.url),p=u.pathname;if(!p.startsWith("/api/"))return env.ASSETS.fetch(request);try{if(p==="/api/admin/sync"&&request.method==="POST"){if(!env.SYNC_TOKEN||request.headers.get("authorization")!==`Bearer ${env.SYNC_TOKEN}`)return json({error:"unauthorized"},401);await ensureSchema(env.DB);return json(await syncProjectionV9(env))}if(request.method!=="GET")return json({error:"method_not_allowed"},405);if(!["/api/health","/api/opportunities","/api/positions","/api/history"].includes(p)&&!p.startsWith("/api/opportunities/"))return json({error:"not_found"},404);await ensureSchema(env.DB);if(p==="/api/health")return json(await health(env));if(p==="/api/opportunities"&&u.searchParams.get("signal_date")){const r=await currentRun(env.DB),date=u.searchParams.get("signal_date");if(date!==r?.as_of_date){const[l,o]=page(u);return json(await historicalOpportunities(env,date,await readPointers(env),{limit:l,offset:o}))}}if(p.startsWith("/api/opportunities/")){const r=await currentRun(env.DB),id=decodeURIComponent(p.slice(19)),x=await env.DB.prepare("SELECT payload FROM dashboard_opportunities_v8 WHERE generation=? AND id=?").bind(r.generation,id).first();return x?json({opportunity:JSON.parse(x.payload),evidence:[]}):json({error:"not_found"},404)}return json(await list(env.DB,u,p.slice(5)))}catch(e){return json({error:e.message==="invalid pagination"?"invalid_pagination":"serving_error",message:e.message},e.message==="invalid pagination"?400:503)}}};