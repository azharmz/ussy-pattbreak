import {createHash} from "node:crypto";
import {gzipSync} from "node:zlib";
const iso=x=>x.toISOString().slice(0,10), signal=new Date("2026-09-17T00:00:00Z"), execution="2026-09-18";
export function fixtureRows(){
 const candidates=[],t1=[],lifecycle=[];
 for(let i=0;i<426;i++){
  const id=`assessment:${String(i+1).padStart(4,"0")}`,security=String(100000+i),pivot=100+i/10,pattern=["CUP_WITH_HANDLE","CUP_WITHOUT_HANDLE","DOUBLE_BOTTOM","FLAT_BASE"][i%4];
  candidates.push({candidate_id:id,security_id:security,signal_date:iso(signal),pattern_type:pattern,pivot_level:pivot,breakout_volume_ratio:1.4+(i%9)/10,pivot_crossed:true,breakout_state:"TECHNICAL_BREAKOUT_CANDIDATE",candidate_stage:"TECHNICAL_BREAKOUT_CANDIDATE",breakout_version:"pattern-breakout-confirmation-v1",morphology_schema:"oneil-pattern-output-v2",morphology_engine:"33-core-p8-frozen-v1"});
  const entry=i<211?"EXECUTED_T1_OPEN":i<306?"BELOW_PIVOT_AT_OPEN":"MISSED_EXTENDED_AT_OPEN",fill=entry==="EXECUTED_T1_OPEN"?execution:null,price=entry==="EXECUTED_T1_OPEN"?pivot*1.02:null;
  t1.push({candidate_id:id,security_id:security,signal_date:iso(signal),candidate_stage:"TECHNICAL_BREAKOUT_CANDIDATE",pivot_level:pivot,next_session_date:execution,next_open:price??(entry==="BELOW_PIVOT_AT_OPEN"?pivot*.99:pivot*1.06),open_extension_pct:price?0.02:null,entry_state:entry,fill_date:fill,fill_price:price,fill_source:fill?"DAILY_OHLCV_OPEN":null});
  const state=i<178?"OPEN":i<211?"EXIT_PENDING":"NOT_OPENED";
  lifecycle.push({position_id:`position:${id}`,candidate_id:id,security_id:security,entry_date:fill,entry_price:price,pivot_level:pivot,state,exit_signal_date:state==="EXIT_PENDING"?execution:null,exit_reason:state==="EXIT_PENDING"?(i<182?"DEFENSIVE_LOSS":"TEN_WEEK_MA_VIOLATION"):null,exit_date:null,exit_price:null,lifecycle_version:"pattern-breakout-lifecycle-v1"});
 }
 return{candidates,t1,lifecycle};
}
const bytes=x=>Buffer.from(x.map(r=>JSON.stringify(r)).join("\n")+"\n"),sha=b=>"sha256:"+createHash("sha256").update(b).digest("hex");
export function fixtureObjects(){const rows=fixtureRows(),objects=new Map;const opportunitySnapshot=rows.candidates.map((x,i)=>({assessment_id:x.candidate_id,base_id:`base:${i+1}`,lineage_id:`lineage:${i+1}`,security_id:x.security_id,ticker:`T${String(i+1).padStart(4,"0")}`,pattern_type:x.pattern_type,morphology_status:"RECOGNIZED",pivot_level:x.pivot_level,as_of_date:x.signal_date,as_of_close:x.pivot_level*(i%3===0?.99:1.01),distance_to_pivot_pct:(i%3===0?-1:1)}));for(let i=0;i<74;i++){const n=427+i,pivot=80+i/10,close=pivot*(.97+(i%3)*.01);opportunitySnapshot.push({assessment_id:`assessment:${String(n).padStart(4,"0")}`,base_id:`base:${n}`,lineage_id:`lineage:${n}`,security_id:String(100000+n-1),ticker:`T${String(n).padStart(4,"0")}`,pattern_type:["CUP_WITH_HANDLE","CUP_WITHOUT_HANDLE","DOUBLE_BOTTOM","FLAT_BASE"][i%4],morphology_status:"RECOGNIZED",pivot_level:pivot,as_of_date:"2026-09-17",as_of_close:close,distance_to_pivot_pct:(close/pivot-1)*100});}const obody=bytes(opportunitySnapshot),ohash=sha(obody),ometa={source_hash:ohash,as_of_date:"2026-09-17",schema_version:"dashboard-opportunity-snapshot-v1",record_count:opportunitySnapshot.length};objects.set("fixture/dashboard-opportunities.jsonl",obody);objects.set("fixture/dashboard-opportunities.json",Buffer.from(JSON.stringify(ometa)));objects.set("pattern-breakout/production/dashboard-opportunities/current.json",Buffer.from(JSON.stringify({jsonl_key:"fixture/dashboard-opportunities.jsonl",metadata_key:"fixture/dashboard-opportunities.json",source_hash:ohash,as_of_date:"2026-09-17"})));const shards=[];for(let i=0;i<opportunitySnapshot.length;i+=100){const logical=bytes(opportunitySnapshot.slice(i,i+100)),stored=gzipSync(logical,{mtime:0}),index=shards.length,key=`fixture/dashboard-serving/${String(index).padStart(5,"0")}.jsonl.gz`;objects.set(key,stored);shards.push({index,key,record_count:Math.min(100,opportunitySnapshot.length-i),logical_sha256:sha(logical),stored_sha256:sha(stored),logical_bytes:logical.length,stored_bytes:stored.length})}const manifest={schema_version:"dashboard-serving-shards-v1",as_of_date:"2026-09-17",source_hash:ohash,record_count:opportunitySnapshot.length,shard_rows:100,shard_count:shards.length,shards},mbody=Buffer.from(JSON.stringify(manifest)),mhash=sha(mbody);objects.set("fixture/dashboard-serving-manifest.json",mbody);objects.set("pattern-breakout/production/dashboard-serving/current.json",Buffer.from(JSON.stringify({schema_version:"dashboard-serving-pointer-v1",as_of_date:"2026-09-17",source_hash:ohash,manifest_key:"fixture/dashboard-serving-manifest.json",manifest_hash:mhash,record_count:opportunitySnapshot.length,shard_count:shards.length})));const membership={snapshot_date:"2026-08-28",count:500,records:opportunitySnapshot.map(x=>({security_id:x.security_id,ticker:x.ticker}))};objects.set("universe/membership/2026-08-28.json",Buffer.from(JSON.stringify(membership)));objects.set("universe/current.json",Buffer.from(JSON.stringify({snapshot_date:"2026-08-28",membership_key:"universe/membership/2026-08-28.json"})));for(const[stage,data]of Object.entries(rows)){const path=stage==="t1"?"t1-execution":stage,key=`fixture/${path}.jsonl`,mkey=`fixture/${path}.json`,body=bytes(data),source_hash=sha(body),as_of_date=stage==="candidates"?"2026-09-17":"2026-09-18",meta={source_hash,created_at:"2026-09-18T00:00:00Z"};if(stage==="t1")meta.upstream_candidate_hash=sha(bytes(rows.candidates));objects.set(key,body);objects.set(mkey,Buffer.from(JSON.stringify(meta)));objects.set(`pattern-breakout/production/${path}/current.json`,Buffer.from(JSON.stringify({jsonl_key:key,metadata_key:mkey,source_hash,as_of_date})));}return objects}
